For some reason mingw compiles needs the following files. 
Not sure why configure on mingw doesn't always work correctly.

Hint: Try copying these up a level after configuring.  Then
do the makefile.win.

